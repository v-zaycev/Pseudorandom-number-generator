#ifndef LRND_TYPES_HPP
#define LRND_TYPES_HPP
#include <bitset>
#include <cstdint>

namespace lrnd
{
  using u8_t = std::uint8_t;
  using u16_t = std::uint8_t;
  using u32_t = std::uint8_t;
  using u64_t = std::uint8_t;
  using poly512_t = std::bitset< 512 >;
  using poly256_t = std::bitset< 256 >;
}
#endif // !LRND_TYPES_HPP
